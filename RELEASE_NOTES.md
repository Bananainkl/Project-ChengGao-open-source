# 澄稿 1.21.7

本版集中修复 DMG 发布验收链，避免开发产物通过但客户实际收到的 DMG 未被验收。

- 发布脚本强制从刚生成的 DMG 安装，不再直接安装 `dist/澄稿.app`。
- 安装前验证 DMG 完整性、安装说明、版本／构建号和 App 签名。
- 启动验收只认当次安装路径的进程，避免其他副本导致假通过。
- 166 项测试通过；真实哔哩哔哩公开搜索以及零历史、零凭证、仅系统 PATH 的 DMG 隔离启动通过。

当前安装包为 Apple Silicon（arm64）本地候选包，使用临时签名，尚未经过 Apple Developer ID 公证。
